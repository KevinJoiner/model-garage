// Code generated by "model-garage" DO NOT EDIT.
package {{ .PackageName }}

import (
	_ "embed"
)

// VSSTableCreateQuery is the SQL query to create a clickhouse table for the {{ .ModelName }} model.
//
//go:embed {{ sqlFileName }}
var VSSTableCreateQuery string

// {{ .ModelName }}ToSlice converts a {{ .ModelName }} to an array of any for Clickhouse insertion.
// The order of the elements in the array is guaranteed to match the order of elements in the `{{ .ModelName }}ColNames`.
func {{ .ModelName }}ToSlice(obj *{{ .ModelName }}) []any {
	return []any{
{{- range .Signals }}
		obj.{{ .GOName }},
{{- end }}
	}
}

// {{ .ModelName }}ColNames returns the column names of the {{ .ModelName }} table.
// The order of the elements in the array is guaranteed to match the order of elements in the `{{ .ModelName }}ToSlice`.
func {{ .ModelName }}ColNames() []string {
	return []string{
{{- range .Signals }}
		"{{ .CHName }}",
{{- end }}
	}
}
// {{ .ModelName }}JSONName2CHName maps the JSON field names to the Clickhouse column names.
var {{ .ModelName }}JSONName2CHName = map[string]string{
{{- range .Signals }}
	"{{ .JSONName }}": "{{ .CHName }}",
{{- end }}
}