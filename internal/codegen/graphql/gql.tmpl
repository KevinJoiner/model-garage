type {{ lower .ModelName }} implments Node {
  {{- range .Signals }}
  """
  {{ .Desc }}
  """
  {{ .JSONName }}: {{ .GQLType }}
    @requiresPrivilege(privileges: {VehicleNonLocationData: null})
  {{- end }}
}